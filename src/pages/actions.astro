---
import Base from '../layouts/Base.astro';
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<head>
  <title>Actions & Campaigns — BoycottWithAI</title>
  <meta name="description" content="Blueprints for consumer boycotts, civil assemblies, and mutual aid. Phase-aligned templates and quickstart guides." />
</head>

<Base>
  <section>
    <h1>{t('actions.title')}</h1>
    <p class="muted">{t('actions.subtitle')}</p>

    <div class="grid" style="margin-top:18px">
      <div class="card">
        <h4>Targeted Economic Pressure</h4>
        <p>Identify economic leverage against firms and institutions supporting rearmament and war industries. Organized divestment and consumer pressure can reduce the incentives for conflict.</p>
      </div>
      <div class="card">
        <h4>Civil Assembly</h4>
        <p>Route planning, legal observers, de-escalation teams, accessibility accommodations, and media liaison scripts — mapped to campaign phases. Focus on peace rallies and solidarity actions that call for disarmament and diplomatic solutions.</p>
      </div>
      <div class="card">
        <h4>Policy & Advocacy</h4>
        <p>Build policy campaigns to push for disarmament measures, restrictions on arms exports, and increased funding for peaceful conflict resolution. Use petitions, lobbying, and public education to shift policy away from militarism.</p>
      </div>
    </div>

    <section style="margin-top:18px">
  <h3>Shareable Templates (Peace & Anti-Rearmament)</h3>
      <p class="muted">Downloadable templates include a phase-aligned checklist, media scripts, and an evaluation sheet to measure impact against objectives.</p>
      <ul>
  <li>Leaflet A4 — English / Italian — policy brief on disarmament</li>
        <li>Press release — editable markdown</li>
  <li>Social media calendar — example CSV with anti-rearmament scripts</li>
      </ul>
    </section>
  </section>
  <section style="margin-top:20px">
    <div class="card">
      <h4>{t('actions.quickstart.title')}</h4>
      <ol>
        {
          (() => {
            const raw = t('actions.quickstart.steps');
            if (Array.isArray(raw)) {
              return raw.map((step) => <li>{step}</li>);
            }
            return <li>{String(raw)}</li>;
          })()
        }
      </ol>
    </div>
  </section>
</Base>
