---
import { getLangFromUrl } from '../i18n/utils';
const lang = getLangFromUrl(Astro.url);
const path = lang === 'en' ? `/en/the-big-short` : `/${lang}/the-big-short`;
return Astro.redirect(path);
---
