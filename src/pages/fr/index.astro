---
import Base from '../../layouts/Base.astro';
import { getLangFromUrl, useTranslations } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<head>
  <title>BoycottWithAI — Outils stratégiques pour des campagnes non violentes</title>
  <meta name="description" content="Outils ouverts, modèles et un manifeste pour organiser une pression économique et civile stratégique. Ressources en plusieurs langues." />
</head>

<Base>
  <div class="hero">
    <div class="left">
      <h1>{t('hero.title')}</h1>
      <p class="lead">{t('hero.subtitle')}</p>
      <p class="muted" style="margin-top:6px">{t('hero.subtitle2')}</p>
      <div class="cta">
        <a class="button" href={`/${lang}/methodology/`}>{t('nav.methodology')}</a>
        <a class="secondary" href={`/${lang}/get-involved/`}>{t('nav.getinvolved')}</a>
      </div>
    </div>
    <div class="card">
      <h3>Alertes</h3>
      <p class="muted">Abonnez-vous aux flux RSS pour des appels urgents et des mises à jour.</p>
      <a class="secondary" href="/rss.xml">S'abonner</a>
    </div>
  </div>

  <section style="margin-top:28px">
    <div class="grid">
      <div class="card">
        <h4>{t('purpose.title')}</h4>
        <p class="muted">{t('purpose.p1')} {t('purpose.p2')} {t('purpose.p3')} {t('purpose.p4')}</p>
      </div>
      <div class="card">
        <h4>Confidentialité & Sécurité</h4>
        <p class="muted">Les choix de conception privilégient la sécurité des participants et le signalement sécurisé : partage sur opt-in, événements éphémères, collecte minimale de données et meilleures pratiques d'anonymat.</p>
      </div>
      <div class="card">
        <h4>Communautaire</h4>
        <p class="muted">Tout ici est destiné à être adapté par des groupes locaux — forkez le projet, contribuez des jeux de données et collaborez sur des stratégies civiques non violentes.</p>
      </div>
    </div>
  </section>

  <section style="margin-top:28px">
    <div class="card">
      <h4>{t('issues.title')}</h4>
      <ul>
        <li>{t('issues.core')}</li>
        <li>{t('issues.escalation')}</li>
        <li>{t('issues.humanitarian')}</li>
        <li>{t('issues.institutions')}</li>
      </ul>
    </div>
  </section>
  <section style="margin-top:28px">
    <div class="card">
      <h4>Du manifeste</h4>
      <p class="muted">{t('manifesto.vision.p1')}</p>
      <div style="margin-top:10px">
        <a class="secondary" href={`/${lang}/manifesto/`}>Lire le manifeste complet</a>
      </div>
    </div>
  </section>
</Base>

